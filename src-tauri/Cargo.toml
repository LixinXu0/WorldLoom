[package]
name = "worldloom-prototype"
version = "0.1.0"
description = "Worldloom research prototype desktop wrapper"
authors = ["Worldloom"]
edition = "2021"

[lib]
name = "worldloom_prototype_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

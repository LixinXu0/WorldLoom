# Worldloom Mock Asset Pack

This directory is reserved for local mock production thumbnails.

The current prototype uses metadata-backed label thumbnails from `src/assets/mockAssetLibrary.ts` because no matching PNG pack is bundled in the repository. Do not fetch external assets for the CHI prototype.
